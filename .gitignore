# Secrets / per-machine config
config.json
config-*.json
# But keep example files
!config-*.example.json

# Pipeline notify config holds camera creds + chat id (keep only the example)
pipeline/dogwatch-notify.config.json

# SSH credentials + Tailscale IP for the homelab server
.ssh

# Generated event clips
clips/
clips-rear-east/

# Debug capture archives (opt-in, see debug_capture.py / README)
debug_captures/

# Downloaded model files
models/

# Python cache
__pycache__/
*.pyc

# OS junk
.DS_Store
Thumbs.db
