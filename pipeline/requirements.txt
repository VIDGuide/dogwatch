# Pipeline scripts run on the host under a plain Python venv (see
# dogwatch-notify.service — currently Python 3.12 on the deployed host),
# independent of the Dockerfile's detector stack. No structural version
# ceiling here: unlike the Dockerfile (see its comment re: ai-edge-litert),
# these were never tied to pycoral/tflite_runtime, so they just track
# current stable releases.
paho-mqtt==2.1.0
requests==2.34.2
Pillow==12.3.0
